<div class="container">
  <div class="menu">
    {{#link-to 'index'}}
      <h1>
        <em>SuperRentals</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to 'about'}}
        About
      {{/link-to}}
      {{#link-to 'contact'}}
        Contact
      {{/link-to}}
      {{#if isSessionEnable}}
        {{#link-to 'rentals.addrental'}}
          Add  Rental
        {{/link-to}}
      {{/if}}
    </div>
  </div>
  <div class="body">
    <div class="jumbo">
      {{outlet}}
    </div>
  </div>
</div>
